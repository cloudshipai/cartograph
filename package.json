{
  "name": "cartograph",
  "version": "0.1.0",
  "description": "Visual codebase mapping plugin for OpenCode - auto-generates architecture diagrams as you code",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun && cd web && npm run build",
    "build:plugin": "bun build src/index.ts --outdir dist --target bun",
    "build:web": "cd web && npm run build",
    "dev": "bun --watch src/index.ts",
    "dev:web": "cd web && npm run dev",
    "typecheck": "tsc --noEmit",
    "postinstall": "cd web && npm install"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  },
  "peerDependencies": {
    "bun": ">=1.0.0"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "opencode",
    "plugin",
    "architecture",
    "visualization",
    "codebase",
    "mapping"
  ],
  "author": "CloudShip AI",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/cloudshipai/cartograph.git"
  }
}
